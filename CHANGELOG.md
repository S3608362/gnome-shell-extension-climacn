# 更新日志

## v1.5 (2026-09-15)

### ⚠️ 安装目录变了

- 扩展标识（UUID）由 `climacn@outlook.com` 改为 **`climacn-weather@outlook.com`**，
  安装目录和打包文件名随之改变。**手动安装过的用户请先删除旧目录**，否则两个版本
  会被同时加载：

  ```bash
  rm -rf ~/.local/share/gnome-shell/extensions/climacn@outlook.com
  ```

- 配置文件按标识存放，因此 **API Key、API Host 与已选城市需要重填一次**。

### 新增

- 空气质量色环：卡片头右侧显示 AQI 数值，环的颜色与长度按 AQI 等级变化。
  需要凭据包含空气质量接口，取不到时整块隐藏，不影响其它数据
- 气压变化趋势：按本地采样计算三小时变化，在气压值后显示 ↑/↓/→。
  采样写入 GSettings，重启 GNOME Shell 后趋势依然可用
- 10 天趋势折线：最高温与最低温两条线，每个点标注温度数值，收在
  「10 天趋势」子菜单里默认不展开。沿用同一个每日预报请求（`days`
  由 3 改为 10），不增加请求次数
- 日出日落弧线：半圆轨道上标出太阳当前位置，两侧显示日出日落时刻。
  数据取自每日预报的 `astro` 字段，同样不增加请求
- 备用数据源：可选启用 Open-Meteo 兜底，在未配置凭据或当日额度用尽时
  改用免费无 Key 的 Open-Meteo，避免界面空白。**默认关闭**——开启后
  会把所选城市的经纬度发送给该第三方服务，界面底部也会标明数据来源

## v1.4 (2026-09-15)

### 新增

- 电池供电时自动刷新间隔由 15 分钟延长到 30 分钟，减少唤醒与请求
- 每日请求额度保护：达到 800 次后暂停自动刷新，次日自动恢复，
  避免超出和风天气每日 1000 次的免费额度
- 手动刷新增加 60 秒冷却，避免连点浪费额度

### 修复

- API Host 现在始终使用 HTTPS
- 校验接口返回的图标代码，避免异常的响应内容影响文件访问
- 修改 API Key 或 API Host 后会自动重新获取数据，无需重载扩展；
  此前凭据出错导致停止刷新后，改正也无法恢复
- 快速连续切换城市时不再出现标题与数据错位

## v1.3 (2026-09-14)

### ⚠️ 破坏性变更

- **必须重新填写 API Host**。和风天气的公共 API 地址（`devapi.qweather.com` 等）
  自 2026 年起逐步停止服务，本版本改用 v1 接口，需要填写你自己账号的 API Host。
  可在[和风天气控制台 - 设置](https://console.qweather.com/setting)查看，形如
  `abcxyz.qweatherapi.com`，无需填写 `https://`。
- **API Key 仍然有效**，不需要换成 JWT。
- **已选城市会重置为北京**。定位方式由城市 ID 改为经纬度，旧配置无法沿用，
  请在菜单里重新搜索并选择城市。

### 新增

- 菜单改为天气卡片样式：顶部为大号温度、天气图标与天气状况
- 详情区改为两列网格，次要数据收进「更多数据」子菜单
- 新增气压、能见度、露点、云量、紫外线指数、阵风、降水量
- 首选项新增「数据来源」分组

### 变更

- 迁移到和风天气 v1 接口（`/weather/v1/current`、`/weather/v1/daily`）
- 温度单位统一，预报不再单独写 `°C`
- 「上次更新」显示最近一次成功更新的时间
- 菜单底部显示数据来源

## v1.2.1

- 修复城市搜索解析错误：363 个城市的省份与城市名显示异常
- 修复港澳城市被错误过滤的问题
- 修复禁用扩展时在途网络请求回调导致的 TypeError
- 城市切换支持外部修改即时生效，无需重载扩展
